{
  "name": "codexresets-net",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "node scripts/validate.mjs && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "node scripts/validate.mjs",
    "deploy": "npm run build && wrangler deploy",
    "test": "node scripts/verify-behavior.mjs",
    "sync:x": "node scripts/sync-x.mjs",
    "dev:worker": "wrangler dev --port 4321"
  },
  "devDependencies": {
    "astro": "^7.3.3",
    "wrangler": "^4.0.0"
  }
}
